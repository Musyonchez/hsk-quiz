# Audit round 4 (Aug 16, 2026): the generic docs/53 playbook, run deliberately

Unlike [52-audit-2026-08-14.md](52-audit-2026-08-14.md) (which ran the repo-adapted
[51-multi-agent-audit-playbook.md](51-multi-agent-audit-playbook.md)), this round runs
[53-comprehensive-audit-prompt-playbook.md](53-comprehensive-audit-prompt-playbook.md) — the
generic, un-adapted ancestor imported from `toolbox` — deliberately as-is, filling in its own
"Project context" section and using its own Phase 2 agent split (A-F) and its own P0-P4 severity
scale, on request. Six parallel read-only agents, each told to check every prior round first
(docs/40-45, docs/50, docs/52) so they report what those missed, not what's already found/fixed.

No app code had changed since docs/52 except one pure file-move (`src/lib/*` → `src/lib/auth/*`,
already reflected in docs/52) — so, same as last round, most of what came back was
re-verification. What's different this time: docs/53's category split cuts the app differently
(e.g. "Security & Performance" together, "UI/UX, Accessibility & Responsive" as one lens with an
explicit contrast-math instruction) than docs/51's five lenses did, and that different cut
surfaced real findings docs/51's split hadn't produced — most notably actual computed WCAG
contrast ratios, which no prior round had run the numbers on.

**Net result: 8 new findings, all P2 or below, zero P0/P1.** No launch-blocking or
security-critical issues found. The two most substantive ones (contrast math, the friend-request
duplicate-row race) are genuinely new information this repo didn't have before this pass.

## Executive summary

- **Health:** good. Four rounds of audits deep, and the codebase keeps coming back clean on
  everything previously checked — the docs/50 reorg and docs/46 hook extraction are holding up
  two rounds later with zero regression.
- **Biggest strength this round exposed:** the quiz-runner state machines (start/pause/replay/
  finish, attempt submission, audio playback concurrency) are more robust than they've been given
  credit for — Agent B specifically hunted for races in all of them and found none beyond what's
  already known.
- **Biggest risk:** a real state-machine bug in the friends feature (finding 1) — a keyboard/
  contrast a11y gap that's genuinely user-facing but not a launch blocker (findings 2-3) — and one
  tier-1 "trust this doc" reference doc that's been quietly wrong since the self-containment pass
  (finding 6).
- **Launch readiness:** nothing here blocks anything; all P2 and below.

## Findings by area

### Bugs & reliability (Agent B)

**1. Friend-request creation race can produce two mirrored pending rows, letting an "ignore"
get silently overridden — P2.**
`src/app/api/friends/requests/route.ts:30-62`, `prisma/schema.prisma:187-203`. The dedup check is
a classic check-then-act TOCTOU (`findFirst` for an existing row between the two users, then
`create` if none found), and the schema's only constraint (`@@unique([userId, friendId])`) is
directional, not symmetric — nothing stops `(A,B,pending)` and `(B,A,pending)` coexisting if A and
B send each other a request in the same race window. If B then ignores A's request, only the A→B
row flips to `"ignored"`; the mirror B→A row stays pending, and A can accept it anyway — the
ignore doesn't stick. Distinct from the already-known, harmless accept/ignore race on a *single*
row (docs/41/52) — this one produces two independent rows that can resolve to contradictory
outcomes. Fix: wrap create in a `$transaction` with serializable isolation, or better, enforce
symmetry at the DB level (canonical-pair unique index). Confidence: likely.

**10. `CustomQuizPicker`'s load/persist effects transiently write `{}` to localStorage on mount,
self-correcting same-tick — P4.** `src/components/quiz/CustomQuizPicker.tsx:106-115`. Real
effect-ordering quirk, near-zero real-world reachability (would need an interrupted page-unload in
a sub-millisecond window). Worth a `hydratedRef` guard only if this file gets touched for other
reasons.

### UI/UX, accessibility & responsive (Agent C)

**2. `--danger`/`--accent-secondary`/`--success` fail WCAG AA contrast as body text — P2.**
`src/app/globals.css:12-28`, used across every auth form's error text, quiz results'
missed/save-failed messages, and the Custom Quiz Picker's success badge. Computed real contrast
ratios (not visual guess) from the actual token hex values: `--danger` on `--background` =
**3.23:1**, on `--surface` = **2.93:1**; `--accent-secondary` on `--background` = **3.63:1**;
`--success` on its own badge surface = **3.65:1**. AA requires 4.5:1 for normal-weight text at
this size. All four fail by 20-35%, while `--foreground` (14.4:1) and `--muted-foreground` (6.3:1)
both pass comfortably — this isn't a systemic dark-theme problem, just these four tokens. `--danger`
specifically is the color used for login/registration/reset-password error text — exactly where
legibility matters most. Fix: brighten the three tokens a few percent luminance, re-verify against
a contrast checker; `--accent` itself needs no change (its real usages all qualify as WCAG "large
text," which only needs 3:1). Confidence: confirmed.

**3. Custom focus indicators on text inputs and the Custom Quiz Picker's checkbox composite to
~1.9:1, well under the ~3:1 a focus indicator needs — P2.** Every auth/friend-request text input
plus both typing-mode quiz runners' pinyin field use `outline-none focus:border-border-strong`,
stripping the native outline and replacing it with `--border-strong`
(`rgba(239,231,218,0.22)`), which composites to **1.87-1.90:1** against the surfaces it sits on.
The Custom Quiz Picker's checkbox (a hidden real `<input>` + styled `<span>`) has the same issue
via `peer-focus-visible:ring-border-strong`. No global `focus-visible` fallback exists anywhere in
`globals.css`, so once the outline is suppressed there's no other visible cue — affecting the
primary interaction surface of the whole site (the pinyin input) for keyboard users. Buttons/links
elsewhere don't override `outline-none` and are unaffected. Fix: either stop suppressing the
native outline, or swap `--border-strong` for something that composites to ≥3:1 on focus.
Confidence: confirmed.

**8. No Tab-key focus trap in `LogoutButton`/`CharacterBrowse` dialogs — P3, re-confirmed
unchanged.** Already tracked as an open finding in docs/52; re-verified this round that no third
dialog-style component has been added since, so the gap's scope hasn't grown. Not a new finding,
listed here for completeness of this round's master table.

### Backend/API & data integrity (Agent D)

**4. `prisma/seed.ts`'s dialog-transcript reseed isn't transactional — P2.** `prisma/seed.ts:
252-264`. `seedTranscripts` does `deleteMany` then `createMany` for a chapter's `DialogLine` rows
as two separate unguarded awaits, unlike every other stale-row cleanup in the same file (which
upsert current data *first*, then delete only what's stale). A process interruption between the
two calls — a dropped connection, a validation error, a killed script — leaves that chapter with
zero `DialogLine` rows, and the consuming page (`.../all/page.tsx:31`) 404s, indistinguishable
from "never had a transcript." Real regression risk, not just staleness. Fix: wrap both calls in
`prisma.$transaction([...])`. Confidence: confirmed. Not P0/P1: `seed.ts` is a manually-run,
`confirmWrite`-gated script, not on any request/CI path, and fully recoverable by re-running.

### Security & performance (Agent E)

**5. `SpeakerButton` ships both audio manifests (word + sentence, ~1,600 entries) to every page,
including ones with no audio feature — P2.** `src/components/SpeakerButton.tsx:11-12`
unconditionally imports both manifests regardless of which `kind` a given call site actually
uses. Verified via a real production build + live network trace (Playwright against `next
start`): both manifests bundle into one shared ~70KB/32KB-gzip chunk that downloads on `/login`
and `/register` — pages with zero `SpeakerButton` instances — at ~18% of `/register`'s total page
JS. Modest at this app's traffic/hosting scale, but real and easy to fix. Fix: split
`SpeakerButton` by `kind` (or lazy-load the manifest via dynamic `import()`), or move the
manifests to static JSON fetched client-side instead of bundled into JS. Confidence: confirmed
(reproduced via real build, not inferred).

**11. `useQuizCountdown`'s interval effect recreates its timer every tick — P4.**
`src/quiz/use-quiz-countdown.ts:32-38`. `secondsLeft` sits in the effect's dependency array even
though the interval callback only uses the functional updater form — so React tears down and
recreates the `setInterval` once a second for the whole quiz duration instead of running one
stable interval. Negligible real cost; flagged as a genuine, cheap fix worth folding into the next
touch of this file, not urgent enough alone.

### Testing, product gaps & DX (Agent F)

**6. `docs/01-overview.md` — a tier-1 "living reference" doc — links to and describes a data
pipeline that's been fully retired — P2.** `docs/01-overview.md:5-7,81-84` links
`characters/words/` and `raw/`, directories that don't exist anywhere in the repo (confirmed via
`git ls-files`), and describes chapter/combined quizzes as reading from markdown/PDF sources —
directly contradicted by `src/lib/extract/extract-chapters.ts`/`extract-combined.ts`'s own
comments, which say everything is now baked into in-repo TS data files. `docs/04-data-pipeline.md`
and `docs/05-architecture.md` already describe the current state correctly; `docs/01-overview.md`
is the one doc that was never updated when the self-containment pass landed, despite being the
doc `docs/README.md`'s tier-1 explicitly tells readers to start with and trust. Fix: rewrite the
"Scope" and "Why two data sources" sections to match docs/04's current framing, fix/remove the
broken links. Confidence: confirmed.

**7. `docs/09-pages.md` describes a per-friend "compare" link that was never built — P3.**
`docs/09-pages.md:113-115` describes each friend row linking into a single-person filtered
leaderboard view; `src/app/friends/page.tsx:66-76` renders each friend as a plain `<UserBadge>`
with no link, and the leaderboard route has no per-friend filter to link to at all. A genuinely
spec'd, not invented, gap — a reader would expect this to exist. Fix: either build the small
one-click compare view, or correct the doc to describe what actually ships (friends-scope
leaderboard tab only). Confidence: confirmed.

**Callout, not a scored finding — the highest-value first automated test, if the no-test-suite
decision (docs/40 item 1) is ever revisited for one surgical exception:**
`src/quiz/pinyin-match.ts`'s `normalizePinyin`/`matchesPinyin` — the actual answer-correctness
gate for two of the four quiz runners, pure and dependency-free, encoding several previously-fixed
edge cases (tone-mark stripping, `v`→`ü`, parenthetical-gloss stripping, slash-alternates) with no
other safety net. Runnable today via Node's built-in `node:test`, zero new dependencies.

**DX / fresh-clone simulation: clean.** README → CONTRIBUTING.md → `.env.example` →
`package.json` scripts → CI → PR template all walked as a fresh contributor would and found
internally consistent, with `.env.example` cross-checked against every real `process.env.*` usage
in the repo. No gap found here beyond finding 6 above (ironically, `README.md`'s own "Getting
started" is accurate where `docs/01-overview.md` is not).

### Architecture & code quality (Agent A)

**9. Dead exported function `getLevelDefinition` — P4.** `src/lib/hsk-level.ts:38-42`. Zero
callers anywhere in the repo (grepped `src/`, `scripts/`, `prisma/`). Either delete it or leave a
comment explaining it's a deliberate seam for near-future levels, matching the pattern the file
already uses for commented-out HSK4-6 slugs.

**Otherwise clean.** The docs/50 `src/components`/`src/lib` reorg and the docs/46 quiz-runner hook
extraction both re-confirmed intact two audit rounds later — no new duplication, no dead routes,
`tsc --noEmit` clean.

## Master issue table

| ID | Severity | Area | Finding | Evidence | Fix effort | Confidence |
|----|----------|------|---------|----------|------------|------------|
| 56-1 | P2 | Bugs/backend | Friend-request race → duplicate mirrored rows, ignore can be overridden | `src/app/api/friends/requests/route.ts:30-62` | Medium (transaction or DB constraint) | Likely |
| 56-2 | P2 | A11y | `--danger`/`--accent-secondary`/`--success` fail WCAG AA contrast | `src/app/globals.css:12-28` | Low (retune 3 hex values) | Confirmed |
| 56-3 | P2 | A11y | Focus-indicator contrast ~1.9:1 on inputs/checkbox | multiple auth/quiz form files | Low-Medium | Confirmed |
| 56-4 | P2 | Backend/data | `seed.ts` DialogLine reseed not transactional | `prisma/seed.ts:252-264` | Low (wrap in `$transaction`) | Confirmed |
| 56-5 | P2 | Performance | Both audio manifests bundled into every page | `src/components/SpeakerButton.tsx:11-12` | Medium (split by kind / lazy-load) | Confirmed |
| 56-6 | P2 | Docs | `docs/01-overview.md` describes a retired data pipeline | `docs/01-overview.md:5-7,81-84` | Low (doc rewrite) | Confirmed |
| 56-7 | P3 | Docs/product gap | `docs/09-pages.md` describes a nonexistent "compare" link | `docs/09-pages.md:113-115` | Low (doc fix) or Medium (build it) | Confirmed |
| 56-8 | P3 | A11y | No Tab focus trap in 2 dialogs (re-confirmed, tracked since docs/52) | `LogoutButton.tsx`, `CharacterBrowse.tsx` | Low (shared `useFocusTrap`) | Confirmed |
| 56-9 | P4 | Code quality | Dead export `getLevelDefinition` | `src/lib/hsk-level.ts:38-42` | Trivial | Confirmed |
| 56-10 | P4 | Bugs | `CustomQuizPicker` transient `{}` localStorage write | `src/components/quiz/CustomQuizPicker.tsx:106-115` | Trivial | Confirmed |
| 56-11 | P4 | Performance | `useQuizCountdown` recreates interval every tick | `src/quiz/use-quiz-countdown.ts:32-38` | Trivial | Confirmed |

## What's already good / don't touch

- The docs/50 folder reorg and docs/46 quiz-runner hook extraction — both re-verified intact by
  Agent A with zero regression two rounds later.
- Every quiz runner's state machine (lifecycle, countdown, attempt submission) and the shared
  audio-player's click-concurrency handling — Agent B traced all of it specifically looking for
  races and found none beyond what's already known and accepted.
- Backend input validation, ownership checks, and error-response shape — re-confirmed clean route
  by route for the fourth round running.
- `.env.example` completeness, CI/CONTRIBUTING.md/PR-template consistency — genuinely accurate,
  no aspirational or unenforced claims.
- The overall design system's *non*-accent colors (`--foreground`, `--muted-foreground`) both pass
  contrast comfortably — the a11y findings above are narrowly scoped to 3-4 specific tokens, not a
  systemic problem with the palette.

## Roadmap

### Soon (P2, all cheap-to-medium effort, no dependencies between them)
- 56-2 + 56-3 together (both are `globals.css` token tuning, worth one PR)
- 56-1 (friend-request race)
- 56-4 (seed.ts transaction wrap)
- 56-5 (audio manifest bundling)
- 56-6 (docs/01 rewrite)

### Later (P3)
- 56-7 (docs/09 compare-link — decide doc-fix vs. build-it first)
- 56-8 (shared focus-trap hook, bundle with 56-3 if convenient — same interaction surface)

### Future / optional (P4, fold into next touch of the relevant file, not worth a dedicated PR)
- 56-9, 56-10, 56-11

## Answering docs/53's closing question

*"If we had to launch this project to real users soon, what are the 10 things we absolutely
should fix first, and why?"* — with only 11 findings total and none above P2, the honest answer
four audit rounds in is: **there isn't a top-10 of launch blockers left**, because there are no
launch blockers. The two worth doing before the others, if picking two: 56-1 (a real, if narrow,
correctness bug in a user-facing trust feature — friends) and 56-2/56-3 together (the only
findings that affect every visitor, including logged-out ones on the auth pages).
